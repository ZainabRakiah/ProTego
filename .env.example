DATABASE_URL=postgresql://user:password@host:port/dbname
# Paste the complete Firebase service-account JSON locally; never commit it.
FIREBASE_CREDENTIALS=
# Add other secrets here if any (e.g. GraphHopper)
